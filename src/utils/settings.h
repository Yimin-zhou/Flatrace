#pragma once

constexpr int m_width = 1280;
constexpr int m_height = 720;

constexpr auto VIEWPORT_WIDTH = 2.4f;
constexpr auto VIEWPORT_HEIGHT = 1.35f;

//constexpr auto DX = VIEWPORT_WIDTH / m_width;
//constexpr auto DY = VIEWPORT_HEIGHT / m_height;

constexpr auto m_tileSize = 16;
constexpr auto m_bundleSize = 4;

//constexpr auto NX = m_width / m_tileSize;
//constexpr auto NY = m_height / m_tileSize;

//constexpr auto N_FRAMES = 1;
//constexpr auto N_RAYS = N_FRAMES * m_width * m_height;

constexpr auto MAX_INTERSECTIONS = 3;

//constexpr auto SPEED = 0.0f;